{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "prisma db push --schema=prisma/schema.sqlite.prisma && prisma generate --schema=prisma/schema.sqlite.prisma",
    "dev": "tsx watch src/index.ts",
    "build": "prisma generate --schema=prisma/schema.postgres.prisma && tsc",
    "start": "node dist/index.js",
    "db:migrate": "prisma migrate deploy --schema=prisma/schema.postgres.prisma",
    "db:push": "prisma db push --schema=prisma/schema.sqlite.prisma",
    "db:generate": "prisma generate --schema=prisma/schema.sqlite.prisma",
    "db:generate:postgres": "prisma generate --schema=prisma/schema.postgres.prisma",
    "db:import-json": "tsx scripts/import-json.ts",
    "pretest": "prisma generate --schema=prisma/schema.sqlite.prisma",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "prisma": "^6.19.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
